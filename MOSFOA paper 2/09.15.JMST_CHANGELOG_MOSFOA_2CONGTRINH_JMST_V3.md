# CHANGELOG — V2.2 → V2.3 → V3

File kết quả: [`09.15.JMST_BAI_BAO_MOSFOA_2CONGTRINH_JMST_APPLICATION_V3.md`](09.15.JMST_BAI_BAO_MOSFOA_2CONGTRINH_JMST_APPLICATION_V3.md).
Các bản trước giữ nguyên, không xoá: V2.2, V2.3 (V2.3 chưa có changelog riêng — gộp vào đây).

**Không có bất kỳ số liệu campaign/kết quả FEM/Pareto/số lần chạy/methodology nào bị thay đổi qua cả 2 vòng vá này.**

---

## PHẦN 1 — V2.2 → V2.3 (người dùng yêu cầu 2026-09-15, trước khi có chỉ thị 12 điểm)

Bổ sung 1 đoạn minh bạch ở mục 2.4 về nguồn gốc/thời điểm tiêu chuẩn của tải trọng trong 2 mô hình FEM — theo đúng khuyến nghị đã trao đổi trước đó ("không nên gọi tải trọng là giả định — nên minh bạch nguồn gốc và thời điểm áp dụng tiêu chuẩn"). Đoạn này sau đó được viết lại chính xác hơn ở V3 (xem Phần 2, mục 3) theo đúng câu chuẩn người dùng cung cấp.

## PHẦN 2 — V2.3 → V3 (chỉ thị 12 điểm, 2026-09-15)

### 1. Định vị lại tinh thần bài báo — ĐÃ ÁP DỤNG
Rà soát toàn bài, xác nhận không có câu nào tạo ấn tượng bài báo là nghiên cứu thiết kế lại/cập nhật 2 công trình theo tiêu chuẩn hiện hành, hay so sánh hệ tiêu chuẩn cũ/mới. Khung "application" đã được khoá từ các vòng vá trước (V2, V2.1, V2.2), vòng này chỉ củng cố thêm bằng đoạn tải trọng/tổ hợp và bảng cơ sở mô hình (xem mục 3–4 dưới).

### 2. Ẩn danh hai công trình — ĐÃ ÁP DỤNG TOÀN BÀI
- Xoá toàn bộ tên địa danh/dự án: "Lạch Huyện", "Cảng cửa ngõ quốc tế Hải Phòng", "VIPGreenPort", "Viconship", "Đình Vũ", "Hải Phòng" — không còn xuất hiện ở bất kỳ đâu trong manuscript (đã kiểm tra lại toàn văn).
- Đổi thống nhất "Công trình A"/"Công trình B" → **"Cầu tàu A"/"Cầu tàu B"** (Wharf A/Wharf B) ở TẤT CẢ vị trí: title (phần sau, mục 12), tóm tắt/abstract VN+EN, mở đầu, tiêu đề mục 2.1/2.2/2.3, bảng 2.3, mục 2.4, 2.5, 3.1, 3.4, mục 5 (quy ước thuật ngữ + các gạch đầu dòng), toàn bộ mục 6 (6.1–6.8, cả tiêu đề mục 6.2/6.3/6.7 lẫn bảng số liệu và văn bản phân tích), kết luận.
- **Giữ nguyên các thông số kỹ thuật/quy mô** (100.000DWT, 30.000DWT, D800-t130, M800, 132 cọc...) vì đây là thông số kỹ thuật phục vụ so sánh case-study, không phải thông tin định danh dự án — đúng tinh thần "quy mô khác nhau" vẫn là trọng tâm của title.
- **Riêng KHÔNG đổi**: cụm "22TCN 207-92, *Công trình bến cảng biển — Tiêu chuẩn thiết kế*" ở tài liệu tham khảo [6] — đây là TÊN CHÍNH THỨC của tiêu chuẩn (trích dẫn nguyên văn), không phải cách gọi công trình A/B, không được sửa.
- Đã thêm đúng nguyên văn ghi chú bảo mật (VN + EN) ở đầu mục 2, trước mục 2.1.
- **Không** gọi hai cầu tàu là "hypothetical" ở bất kỳ đâu — xác nhận rà soát không có cụm này.

### 3. Cách trình bày tải trọng và tổ hợp — ĐÃ ÁP DỤNG
Thay đoạn tải trọng đã viết ở V2.3 bằng đúng câu chuẩn được cung cấp trong chỉ thị (mục 2.4, đoạn 2), chỉ thay "công trình A/B" → "Cầu tàu A/B" cho khớp ẩn danh. Câu tiếng Anh tương ứng **chưa được dịch/chèn** vào manuscript (bài hiện tại thuần Việt ở phần nội dung, chỉ có tóm tắt/abstract song ngữ) — nếu cần bản dịch tiếng Anh đầy đủ của đoạn này (ví dụ khi nộp cho phiên bản quốc tế), cần yêu cầu thêm.

### 4. "Assumed loads" — ĐÃ KIỂM TRA, KHÔNG PHÁT SINH
Rà soát manuscript: không có cụm "tải trọng giả định"/"assumed loads"/"hypothetical loads" ở bất kỳ đâu (đã tránh từ vòng trao đổi trước khi vá V2.3). Cách gọi hiện tại nhất quán theo hướng "tải trọng... kế thừa nguyên trạng từ hồ sơ thiết kế/khai thác gốc" — đúng tinh thần "project-based load cases" được yêu cầu.

### 5. Không gán tiêu chuẩn hiện hành vào campaign đã chạy — ĐÃ KIỂM TRA
Xác nhận: TCVN 10304:2025 (mục 3.4) là tiêu chuẩn **thật sự được dùng để tính ràng buộc địa kỹ thuật trong toàn bộ campaign** (đã xác nhận trực tiếp trong code `evaluate_pile_design.m`/`project_config.m` ở phiên làm việc trước) — đây KHÔNG phải trường hợp "gán tiêu chuẩn hiện hành retrofit vào kết quả cũ", mà là tiêu chuẩn áp dụng thật cho ràng buộc tối ưu, tách bạch rõ với các tiêu chuẩn tải trọng/móng cọc gốc của mô hình FEM (mục 2.4, mang tính lịch sử/kế thừa). Hai phạm vi này đã được viết tách bạch trong 2 đoạn khác nhau (2.4 vs 3.4), không gây nhầm lẫn.

### 6. Vai trò tiêu chuẩn cũ — ĐÃ ÁP DỤNG
22TCN 222-95, 22TCN 207-92, TCVN 9386:2012, TCVN 10304:2014, TCXD 205:1998 được mô tả trong bảng mới (mục 4 dưới) và trong đoạn văn mục 2.4 là "cơ sở thiết kế gốc"/nguồn tham chiếu của mô hình số — có ghi chú tường minh dưới bảng: *"không nên hiểu là khung tiêu chuẩn hiện hành được áp dụng để thiết kế lại các cầu tàu"*. Không gọi bất kỳ tiêu chuẩn nào trong nhóm này là "hiện hành".

### 7. Bảng cơ sở mô hình số — ĐÃ THÊM
Bảng mới trong mục 2.4 (chưa đánh số "Bảng X" cụ thể trong `.md` — xem ghi chú dưới), 9 hàng đúng theo đề xuất: Mô hình FEM, Trường hợp tải trọng, Tổ hợp tải trọng, Cơ sở tải trọng tàu/hàng hải, Cơ sở thiết kế bến cảng, Cơ sở kháng chấn, Cơ sở móng cọc gốc, Nền tảng phân tích FEM, Biến tối ưu — 2 cột Cầu tàu A/B. Ghi chú dưới bảng dùng đúng tinh thần câu đề xuất (dịch sang tiếng Việt cho khớp ngôn ngữ manuscript).

### 8. Thay "verification/kiểm chứng" — ĐÃ ÁP DỤNG
- Title: bỏ "KIỂM CHỨNG TRÊN HAI CÔNG TRÌNH..." → "NGHIÊN CỨU TRÊN HAI CẦU TÀU..." (đúng nguyên văn đề xuất, cả VN và EN).
- Mở đầu: "kiểm chứng bước đầu trên một mô hình..." → "áp dụng bước đầu trên một mô hình..."; "đã được trình bày và kiểm chứng đầy đủ trong công trình đó" → "đã được trình bày và áp dụng minh hoạ đầy đủ trong nghiên cứu đó" (đồng thời sửa luôn "công trình đó" — nghĩa gốc là "nghiên cứu/công trình khoa học [1]", không phải Cầu tàu A/B — đổi thành "nghiên cứu đó" để tránh nhầm lẫn với thuật ngữ ẩn danh mới).
- "chỉ kiểm chứng trên một case-study đơn lẻ" → "chỉ áp dụng, đánh giá trên một case-study đơn lẻ".
- Đoạn 2.3 kết: "phục vụ mục tiêu kiểm chứng khả năng áp dụng của MOSFOA" → "phục vụ mục tiêu đánh giá khả năng áp dụng của MOSFOA".
- Không còn cụm "kiểm chứng" nào mang nghĩa "validation thuật toán" trong toàn bài (đã grep lại toàn văn xác nhận).

### 9. Giữ nguyên phương pháp và kết quả đã khoá — XÁC NHẬN KHÔNG ĐỔI
33 catalogue alternatives, 18 phương án khả thi/cầu tàu, 2 objectives, SAP2000 FEM, workflow OAPI, Npop=8, Max_it=20, 168 FE/run, 20 runs/cầu tàu, 40 runs tổng, tập Pareto tham chiếu 6 điểm/cầu tàu, 100% recovery, toàn bộ giá trị f1/f2 trong các bảng — **giữ nguyên tuyệt đối**, chỉ thay danh xưng "Công trình" → "Cầu tàu" trong text/header, không chạm vào bất kỳ con số nào. Không thêm N-M interaction, không thêm SSI/cập nhật Ks hay L_fix theo D — xác nhận lại đúng như các vòng trước.

### 10. Giới hạn nghiên cứu — ĐÃ BỔ SUNG 1 GẠCH ĐẦU DÒNG MỚI
Thêm 1 bullet mới trong mục 6.8 (Hạn chế): nêu rõ tải trọng/tổ hợp/một phần tiêu chuẩn móng cọc gốc được kế thừa từ thời điểm thiết kế ban đầu, nghiên cứu không cập nhật/thiết kế lại theo tiêu chuẩn hiện hành, kết quả phản ánh ứng xử của mô hình số đã thiết lập — không phải hồ sơ thiết kế thi công mới. Các bullet khác giữ nguyên.

### 11. Kiểm tra câu dễ gây hiểu nhầm — ĐÃ RÀ SOÁT
Grep + đọc lại toàn văn cho các cụm rủi ro (tiêu chuẩn 2026, hồ sơ thiết kế mới, tải trọng tự giả định, công trình giả định, MOSFOA tốt hơn thuật toán khác, validation FEM model) — **không phát hiện câu nào** thuộc các dạng này trong V3.

### 12. Không thay đổi kết quả chỉ vì cập nhật tiêu chuẩn — XÁC NHẬN TUÂN THỦ
Không tính lại tải trọng, không đổi load combinations, không đổi mô hình địa kỹ thuật, không chạy lại MOSFOA. Toàn bộ thay đổi ở V3 là biên tập văn bản (đổi danh xưng, viết lại 1 đoạn văn, thêm 1 bảng, thêm 1 bullet hạn chế, đổi 3 tiêu đề) — không đụng đến file code (`code/*.m`) hay file kết quả (`code/results/*.mat`).

---

## Những gì CHƯA làm ở V3 (cần làm tiếp nếu người dùng yêu cầu)

1. **File `.docx`** (`09.15.JMST_..._V2.2.docx`) và **3 hình PNG** trong `Hinh_minh_hoa/` vẫn dùng nhãn "Công trình A/B" — chưa được cập nhật để khớp với bản V3 ẩn danh. Cần dựng lại `.docx` từ nội dung V3 (đổi nhãn bảng, chèn bảng "Cơ sở mô hình số" mới, chèn 2 tham khảo tiêu chuẩn mới [7][8][9]) và đổi nhãn "Công trình A/B" → "Cầu tàu A/B" trong 3 hình trước khi dùng bản cuối.
2. Bảng mới ở mục 2.4 làm bài dài thêm — chưa kiểm tra lại giới hạn 7 trang JMST (chỉ kiểm tra được khi build `.docx`).
3. Bản dịch tiếng Anh đầy đủ của đoạn "Numerical Models / Load Cases and Combinations" (mục 4 chỉ thị) — hiện chỉ có bản tiếng Việt trong thân bài; bản tiếng Anh người dùng cung cấp chưa được chèn vào đâu (không có mục song ngữ toàn văn trong manuscript hiện tại, chỉ tóm tắt là song ngữ).
4. Tài liệu tham khảo [7] TCVN 9386:2012, [8] TCVN 10304:2014, [9] TCXD 205:1998 mới thêm — tên đầy đủ dựa trên hiểu biết chung về các tiêu chuẩn này (không tự bịa số hiệu, đây là các tiêu chuẩn thật đã biết), nhưng **chưa đối chiếu trực tiếp với văn bản gốc** trong dự án — nên xác nhận lại trước khi nộp chính thức.
